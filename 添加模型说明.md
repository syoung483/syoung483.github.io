# 添加新模型说明

## 已完成的配置

✅ 已创建通用模型查看器 (`model-viewer-universal.html` 和 `model-viewer-universal.js`)
✅ 已更新设备选择页面 (`usage-select.html`)
✅ 已更新图片识别功能（前端和后端）

## 需要手动完成的步骤

### 1. 复制模型文件

请将以下三个GLB文件复制到项目的 `models` 目录：

```
源文件位置：
- D:\project\场外报警2.glb
- D:\project\灭火器.glb
- D:\project\宿舍.glb

目标位置：
- D:\project\syoung483.github.io-main\models\场外报警2.glb
- D:\project\syoung483.github.io-main\models\灭火器.glb
- D:\project\syoung483.github.io-main\models\宿舍.glb
```

### 2. 复制方法

**方法一：使用文件管理器**
1. 打开文件管理器
2. 找到源文件：`D:\project\`
3. 复制以下文件：
   - `场外报警2.glb`
   - `灭火器.glb`
   - `宿舍.glb`
4. 粘贴到：`D:\project\syoung483.github.io-main\models\`

**方法二：使用命令行**
```powershell
# 在PowerShell中执行
copy "D:\project\场外报警2.glb" "D:\project\syoung483.github.io-main\models\场外报警2.glb"
copy "D:\project\灭火器.glb" "D:\project\syoung483.github.io-main\models\灭火器.glb"
copy "D:\project\宿舍.glb" "D:\project\syoung483.github.io-main\models\宿舍.glb"
```

## 使用方法

### 查看模型

1. **通过设备选择页面**：
   - 访问 `usage-select.html`
   - 点击对应的设备卡片按钮

2. **直接访问**：
   - 场外报警：`model-viewer-universal.html?model=场外报警2`
   - 灭火器：`model-viewer-universal.html?model=灭火器`
   - 宿舍：`model-viewer-universal.html?model=宿舍`

3. **通过图片识别**：
   - 使用图片识别功能识别设备后，会自动跳转到对应的3D模型页面

## 模型配置

模型配置在 `model-viewer-universal.js` 中的 `modelConfig` 对象：

```javascript
const modelConfig = {
    '场外报警2': {
        path: 'models/场外报警2.glb',
        title: '🚨 场外报警设备',
        scale: 5
    },
    '灭火器': {
        path: 'models/灭火器.glb',
        title: '🧯 灭火器',
        scale: 5
    },
    '宿舍': {
        path: 'models/宿舍.glb',
        title: '🏠 宿舍模型',
        scale: 5
    }
};
```

如果需要调整模型的显示大小，可以修改 `scale` 值。

## 注意事项

1. 确保模型文件名与配置中的路径完全一致（包括大小写）
2. 如果模型文件较大，加载可能需要一些时间
3. 确保通过HTTP服务器访问，直接打开HTML文件可能无法加载模型
